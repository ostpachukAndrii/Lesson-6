import { AppDeliveryDateValidatorDirective } from './app-delivery-date-validator.directive';

describe('AppDeliveryDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDeliveryDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
